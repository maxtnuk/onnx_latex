(
    symbol: "h",
    n_type: Activation,
    entries: {
        "Sigmoid":(
            inputs: 1,
            formul: "\\frac{1}{1+e^{-(#)}}",
            diff: Some("$(1-$)")
        ),
        "Clip":(
            inputs: 1,
            formul: "max(0,#)",
            diff: Some("bt(0,&)")
        )
    }
)